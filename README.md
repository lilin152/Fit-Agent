20260727
1.使用uv创建了基本的环境 
2.今日代码，写出基本的AI-agent代理,写了基本的逻辑函数(其基本逻辑仍未完善)植入主干tools中，包括，后续需要设置测试计划测试这六个函数generate_weekly_plan ,get_user_profile,log_training,get_training_record,update_user_profile 
20260728
1. 创建了数据库基本的key，注入了几条常规的数据；
2.修改了generate_weekly_plan逻辑，还未测试基本逻辑是否正确；
3.需要确定引入RAG，非结构化数据，而下载的健身动作是结构化动作，不需要，如果要引入基本的rag作为基本的结构，则需要专业性健身数据。（这个不引入第一版）；
